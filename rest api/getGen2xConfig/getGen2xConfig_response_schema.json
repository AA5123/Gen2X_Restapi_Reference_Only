{
    "title": "Get Gen2X Configuration Success Response",
    "description": "Returns the current Gen2X configuration. Only the latest configured feature is included.",
    "type": "object",
    "properties": {
        "tagFocus": {
            "type": "object",
            "properties": {
                "enabled": { "type": "boolean" }
            },
            "required": ["enabled"]
        }
    },
    "required": ["tagFocus"]
}