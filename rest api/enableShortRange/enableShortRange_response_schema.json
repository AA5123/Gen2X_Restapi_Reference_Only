{
    "title": "Enable Protected Mode with Short Range Success Response",
    "description": "From PUT /cloud/impinjGen2X 200 response",
    "type": "object",
    "properties": {
        "message": {
            "type": "string"
        }
    },
    "required": ["message"],
    "examples": [
        {
            "message": "Success: Gen2X configured. Use applyImpinjGen2X flag in start command to apply features."
        }
    ]
}